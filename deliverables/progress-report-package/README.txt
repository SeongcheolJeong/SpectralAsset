Progress Report Package

Open:
- progress-report-slides.html

Recommended:
- Extract the zip first, then open the HTML file from the extracted folder.

What is included:
- standalone presentation HTML
- PNG previews used in the sign gallery
- PNG previews used in the 3D asset coverage slide
- referenced GLB, SVG, USDA, and source-policy files

Notes:
- keep the folder structure unchanged
- open the HTML after extraction, not from inside the zip viewer
- the slide deck now uses static PNG previews for 3D asset cards, so it does not require WebGL support to show those images
